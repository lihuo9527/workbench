import { DownUpdateDirective } from './down-update.directive';

describe('DownUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new DownUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
