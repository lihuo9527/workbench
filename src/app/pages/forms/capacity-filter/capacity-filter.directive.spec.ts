import { CapacityFilterDirective } from './capacity-filter.directive';

describe('CapacityFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new CapacityFilterDirective();
    expect(directive).toBeTruthy();
  });
});
